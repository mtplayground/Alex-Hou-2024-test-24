export const lessonSections = [
  { id: "moving-with-load", title: "Moving With the Load" },
  { id: "supporting-segments", title: "Supporting Segments" },
  { id: "practice", title: "Practice" },
];

# Movable Pulley

<LessonSection id="moving-with-load" title="Moving With the Load">
  <p>
    A movable pulley travels with the load. Because the load is supported by
    more than one rope segment, the input force can be smaller than the load
    force.
  </p>
</LessonSection>

<LessonSection id="supporting-segments" title="Supporting Segments">
  <p>
    Count the rope segments holding the load. That count helps you estimate the
    ideal mechanical advantage.
  </p>
  <PulleyDemo label="Movable pulley lesson demo" />
</LessonSection>

<LessonSection id="practice" title="Practice">
  <Quiz
    quizId="lesson-movable-pulley-check"
    title="Supporting Rope Segments"
    prompt="If two rope segments support the load, what happens in an ideal movable pulley?"
    correctOptionId="a"
    explanation="Two supporting rope segments can roughly double the mechanical advantage, so the input force can be about half the load force."
    options={[
      {
        id: "a",
        label: "A",
        text: "The input force can be about half the load force",
      },
      { id: "b", label: "B", text: "The pulley stops moving with the load" },
      { id: "c", label: "C", text: "The rope no longer changes direction" },
    ]}
  />
</LessonSection>
